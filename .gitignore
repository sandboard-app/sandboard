/target
/sandboard.json
/sandboard.json.tmp
.sandboard/
node_modules
web/dist
.DS_Store
web/tsconfig.tsbuildinfo

# UI screenshots, regenerated on demand. docs/images is where CI drops them
# for mdBook; the published book is the only place they are meant to live.
web/shots
docs/images
.sandboard

# Local Dolt / sqlite artifacts (not board state under .sandboard/)
.dolt/
*.db

# Local sandbox vendor blobs (install via Containerfile / CLI)
sandbox/agy.tar.gz
sandbox/antigravity

# Local board backups
sandboard.json.pre-flat-backup

# mdBook Mermaid browser assets — fetched by `mdbook-mermaid install` (make docs / CI).
mermaid.min.js
mermaid-init.js
